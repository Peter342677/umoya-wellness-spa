PORT=3070

# Optional SMTP config. If unset, contact form submissions are logged to
# data/submissions/submissions.json instead of emailed.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
CONTACT_TO_EMAIL=umoyahelp@gmail.com
